import { brandIcon } from "@/lib/brand-icon";

// Standard ("any" purpose) PWA icon referenced by the web app manifest.
export const dynamic = "force-static";

export function GET() {
  return brandIcon(192);
}
